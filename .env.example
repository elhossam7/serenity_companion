VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SENTRY_DSN=
VITE_UMAMI_URL=
VITE_UMAMI_WEBSITE_ID=
# Serenity Companion — Environment Variables (example)

# Required by src/lib/supabase.js
# Get these from Supabase → Project Settings → API
VITE_SUPABASE_URL="https://YOUR_PROJECT_REF.supabase.co"
VITE_SUPABASE_ANON_KEY="YOUR_PUBLIC_ANON_KEY"

# Optional: where Supabase should redirect after auth (Dev only)
# VITE_REDIRECT_URL="http://localhost:4028"

# Do NOT put any AI provider keys here. Keep AI secrets in Supabase Edge Function secrets only.
# Rename to .env.local or .env and fill values

# Supabase Project Config
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: Explicit redirect for password recovery
# Must exactly match an entry in Supabase Auth -> URL Configuration -> Additional Redirect URLs
# For local dev (Vite default server):
# VITE_AUTH_REDIRECT_URL=http://localhost:4028/reset-password
VITE_AUTH_REDIRECT_URL=
# Copy to .env and fill with your project val
# Optional: Third-party API keys (do NOT commit real keys)
# VITE_OPENAI_API_KEY="sk-..."

# App
# VITE_APP_ENV="development"
